# Meta-SHAP
Meta-SHAP is a disease-agnostic framework for deconstructing treatment effect heterogeneity from aggregate-level RCT data. It integrates multilevel meta-regression (R) with an inverse-variance weighted XGBoost-SHAP pipeline (Python) to identify and validate clinical predictors.
